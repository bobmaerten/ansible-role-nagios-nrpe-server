---
galaxy_info:
  author: Bob Maerten
  description: Nagios Remote Plugin Executor daemon and a few non packaged checks
  license: MIT
  min_ansible_version: 1.2
  platforms:
    - name: Debian
      versions:
        - wheezy
  categories:
    - monitoring
    - system
dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.

